const { createBudget } = require('../services/budgetService');

const createBudget = async (req, res) => {
    const { budgetYear } = req.body
    
    console.log(`Request to create budget by '${username}' and password: '${password}'`);

}